<div class="row">
  <h4>My Friends (DEPRECATED):</h4>
  {{#if friendships}}
    {{#each friendships as |friendship|}}
      <p>{{friendship.user.name}}</p>
    {{/each}}
  {{else}}
    <h5>You have no friends yet :(</h5>
  {{/if}}
</div>
